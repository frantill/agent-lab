[project]
name = "agent-lab"
version = "0.1.0"
description = "Lab per costruire agenti AI piccoli, mirati e replicabili (Pydantic AI + OpenRouter)"
requires-python = ">=3.11"
dependencies = [
    "pydantic-ai",
    "python-dotenv",
]

[tool.uv]
package = false
